# Optional. Only needed if you deploy backend/cloudflare-worker.
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional frontend override. Put this in frontend/.env.local when the Worker URL changes.
VITE_LIFEOS_AI_PROXY_URL=https://lifeos-proxy.gaopengju.workers.dev/api/parse-journal
